# nvim_report

This repository records my Neovim configuration history.

## Policy
- Pure Neovim base first
- Plugins are added only when necessary
- Every change must be reproducible
- Local scripts are ignored by .gitignore

## Structure
- nvim/init.lua : Neovim configuration
- ig_script/    : Local only (ignored by git)

